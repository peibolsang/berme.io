interface:
  display_name: "Berme Scrollytelling"
  short_description: "Build a scroll story and update its GitHub Issue"
  default_prompt: "Use $berme-scrollytelling to build and validate a custom scroll story for GitHub Issue #<number>, then update the Issue body."
